---
collections:
  - name: community.hashi_vault   # AppRole login + KV v2 reads (vault_certs)
  - name: community.general       # jenkins_plugin / jenkins_script (jenkins_controller, jenkins_docker_cloud)
  - name: community.crypto        # from-scratch CA/server/client cert generation (molecule/integration prepare.yml)
